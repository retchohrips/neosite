{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run dev server on startup",
			"type": "shell",
			"command": "~/.bun/bin/bun run dev",
			"presentation": {
				"reveal": "always",
				"panel": "dedicated"
			},
			"runOptions": { "runOn": "folderOpen" }
		}
	]
}
